# Safety constraint for SQLite data consistency
replicaCount: 1

commonLabels:
  app.kubernetes.io/environment: "production"

image:
  repository: ghcr.io/nexu-io/od
  pullPolicy: IfNotPresent
  tag: "latest"

config:
# The runtime environment for the Node.js process. 
# Must be either "production" or "development". 
# For Staging clusters, leave this as "production".
  nodeEnv: "production"
  allowedOrigins: ""
  publicBaseUrl: ""
  nodeOptions: "--max-old-space-size=192"
  webPort: 7456
  bindHost: "127.0.0.1"
  apiToken: "secure-default-token-change-me"

service:
  type: ClusterIP
  port: 80

persistence:
  enabled: true
  storageClass: ""
  accessMode: ReadWriteOnce
  size: 10Gi

ingress:
  enabled: false
  className: "nginx"
  annotations:
    # Critical: Disables proxy buffering so Server-Sent Events (SSE) stream in real-time
    nginx.ingress.kubernetes.io/proxy-buffering: "off"
    nginx.ingress.kubernetes.io/proxy-read-timeout: "600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "600"
  hosts:
    - host: open-design.local
      paths:
        - path: /
          pathType: ImplementationSpecific
  tls: []
  ## example for tls configuration:
  #  - secretName: open-design-tls
  #    hosts:
  #      - open-design.local

resources:
  limits:
    cpu: 1000m
    memory: 1024Mi
  requests:
    cpu: 200m
    memory: 256Mi

containerSecurityContext:
  seLinuxOptions: {}
  runAsUser: 1001
  runAsGroup: 1001
  runAsNonRoot: true
  privileged: false
  readOnlyRootFilesystem: true
  allowPrivilegeEscalation: false
  capabilities:
    drop: ["ALL"]
  seccompProfile:
    type: "RuntimeDefault"

podSecurityContext:
  fsGroupChangePolicy: Always
  sysctls: []
  supplementalGroups: []
  fsGroup: 1001

authProxy:
  image: "nginxinc/nginx-unprivileged:1.25-alpine-slim"
  port: 8080
  securityContext:
    runAsUser: 101
    runAsGroup: 101
    runAsNonRoot: true
    readOnlyRootFilesystem: true
    allowPrivilegeEscalation: false
    capabilities:
      drop: ["ALL"]

livenessProbe:
  enabled: true
  initialDelaySeconds: 20
  periodSeconds: 30
  timeoutSeconds: 5
  failureThreshold: 3

readinessProbe:
  enabled: true
  initialDelaySeconds: 5
  periodSeconds: 10
  timeoutSeconds: 5
  failureThreshold: 3

# WARNING: Do not enable HPA if using the default SQLite storage. 
# Concurrent writes from multiple pods will corrupt the database file.
hpa:
  enabled: false
  minReplicas: 1
  maxReplicas: 5
  targetCPUUtilizationPercentage: 80
  targetMemoryUtilizationPercentage: 80

nodeSelector: {}
affinity: {}
tolerations: []

initContainers: []
sidecars: []

# --- Local additions (not in upstream) ---------------------------------------
# Extra environment variables for the MAIN open-design container. Appended after
# the built-in env, so anything here can also override earlier vars.
extraEnvs: []
  # Persist Codex auth/config across pod restarts by pointing CODEX_HOME at the
  # daemon's PVC (/app/.od is the only persistent, writable path since the
  # rootfs is read-only):
  # - name: CODEX_HOME
  #   value: /app/.od/codex

# Extra volumes / volume mounts for the MAIN container.
# NOTE: readOnlyRootFilesystem is true, so an agent can only write to a mounted
# volume. The simplest durable Codex home is a subdir of the existing `storage`
# PVC (no extra PVC needed):
#   extraVolumeMounts:
#     - name: storage
#       mountPath: /codex
#       subPath: codex
#   extraEnvs:
#     - name: CODEX_HOME
#       value: /codex
# For a separate PVC instead, create it out-of-band and reference it here.
extraVolumes: []
extraVolumeMounts: []
