# Default values for home-assistant.
# Rendering is delegated to the `common` library chart; this file only supplies
# the per-application data. See charts/common for the full template.

replicaCount: 1

image:
  repository: linuxserver/homeassistant
  pullPolicy: IfNotPresent
  # Empty => the chart's appVersion is used (pins the deploy). This is the same
  # image `latest` currently resolves to (identical digest).
  tag: ""

service:
  type: ClusterIP
  port: 8123

ingress:
  enabled: false
  className: ""
  annotations: {}
  hosts: []
  #   - host: home.example.com
  tls: []
  #   - hosts:
  #       - home.example.com
  #     secretName: home-assistant-tls   # optional; omit when the controller manages certs
  # path: /            # optional, default "/"
  # pathType: Prefix   # optional, default "Prefix"

persistence:
  enabled: true
  # Cluster default StorageClass when empty.
  storageClass: ""
  size: 10Gi
  accessModes:
    - ReadWriteOnce
  volumes:
    config:
      mountPath: /config
      # Empty suffix => PVC is named exactly "<release>".
      claimNameSuffix: ""

resources: {}
  # requests:
  #   cpu: 100m
  #   memory: 256Mi
  # limits:
  #   cpu: "2"
  #   memory: 2Gi

nodeSelector: {}
tolerations: []
affinity: {}

# Additional environment variables.
extraEnvs: []
  # - name: TZ
  #   value: "America/New_York"

# Load ALL keys from an existing secret as environment variables.
existingSecret:
  enabled: false
  name: ""

# Free-form extra volumes / mounts (e.g. a USB device or dbus socket).
extraVolumes: []
extraVolumeMounts: []
