apiVersion: v1
kind: Service
metadata:
  name: {{ include "javalin.fullname" . }}
  labels:
    {{- include "javalin.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "javalin.selectorLabels" . | nindent 4 }}
