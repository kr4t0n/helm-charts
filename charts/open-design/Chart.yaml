apiVersion: v2
name: open-design
description: A Helm chart for deploying Open Design on Kubernetes
type: application
# Local version: upstream chart 0.1.0 + this repo's extraEnv/extraVolume
# passthrough (see README provenance banner). Bump the patch on local changes.
version: 0.1.1
appVersion: "0.7.0"
