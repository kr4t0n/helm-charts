apiVersion: v2
name: code-server
description: A Helm chart for Kubernetes

type: application
version: 0.1.1
# Pinned to the current linuxserver tag (identical digest to `latest`). The
# deployed tag comes from values.yaml image.tag.
appVersion: 4.126.0

dependencies:
  - name: common
    version: 0.2.2
    repository: file://../common
