apiVersion: v2
name: moviepilot
description: A Helm chart for Kubernetes

type: application
version: 0.1.1
# Kept at the chart's existing pin (current upstream is much newer, e.g. 2.14.x
# — bump this deliberately when you want to upgrade). The deployed tag comes
# from values.yaml image.tag.
appVersion: 2.9.23

dependencies:
  - name: common
    version: 0.2.2
    repository: file://../common
