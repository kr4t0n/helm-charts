apiVersion: v2
name: qbittorrent
description: A Helm chart for Kubernetes

type: application
version: 0.1.0
# Kept at the chart's existing pin (current upstream is newer, e.g. 5.2.2 —
# bump this deliberately when you want to upgrade). The deployed tag comes from
# values.yaml image.tag.
appVersion: 5.1.4

dependencies:
  - name: common
    version: 0.2.1
    repository: file://../common
