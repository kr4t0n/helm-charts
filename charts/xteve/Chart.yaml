apiVersion: v2
name: xteve
description: M3U proxy server for Plex, Emby and other IPTV/.M3U8 clients (xTeVe)

type: application
version: 0.1.2
# xTeVe app version. The image only publishes `latest`, so this is used for the
# version label; the deployed tag is set in values.yaml.
appVersion: "2.2.0"

dependencies:
  - name: common
    version: 0.2.2
    repository: file://../common
